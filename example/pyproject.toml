[project]
name = "example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12, <4.0"
dependencies = []

[dependency-groups]
dev = [
    "slang",
]

[tool.uv.sources]
slang = { workspace = true }
